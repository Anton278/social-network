import styled from "@emotion/styled";
import Link from "next/link";

export const User = styled(Link)({
  display: "flex",
  alignItems: "center",
  textDecoration: "none",
  color: "#000",
  width: "fit-content",
});
